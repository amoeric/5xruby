<h1>修改任務</h1>
<%= link_to "返回", root_path%>
<%= form_for @mission do |mission|%>
    <%= mission.label :name %>
    <%= mission.text_field :name %>
    <%= mission.label :content %>
    <%= mission.text_field :content %>
    <%= mission.submit "送出" %>
<%end%>