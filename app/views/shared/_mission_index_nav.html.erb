<nav class="top-list">
  <div class="nav-list-left">
    <%= link_to  t("nav.mission_list"), missions_path %>
  </div>
  <div class="nav-list-middle">
    <%= render 'shared/searchform' %>
  </div>
  <div class="nav-list-right">
    <%= link_to  t("nav.new_mission"), new_mission_path %>
    <ul class="nav-list-ul">
      <div class="user-info">
        <%= "Hi,#{current_user.email}▿ "%>
        <div class="user-avatar"><%= image_tag current_user.avatar_url %></div>
      </div>
      <li><%= link_to t("user.profile"), user_path(current_user) %></li>
      <li><%= link_to t("user.logout"), logout_path(current_user), method: 'delete' %></li>
      <li><%= link_to t("admin.home"), admin_root_path if current_user.admin? %></li>
    </ul>
  </div>
</nav>